import { decodeNetworkPacket } from "../networkPacket";
import { generateFakeEthernetFrameBuffer } from "./ethernetFrame.mock";

describe("Network packet decoder", () => {
  it("Can parse TTL", () => {});

  it("Can parse protocol", () => {});

  it("Can parse source IP", () => {});

  it("Can parse destination IP", () => {});

  it("Can parse options", () => {});

  it("Can extract payload", () => {});

  it("Throws error on invalid ethernet frame", () => {});
});
